{
    "tasks": [
        {
            "type": "shell",
            "label": "g++.exe build active file",
            "command": "make",
            "args": [
                "DEBUG = true"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "g++ build & run active file",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}